# 로컬 개발 전용 환경변수
# Next.js 서버가 /api/* 요청을 백엔드(https://localhost:8000)로 프록시할 때,
# Node.js 런타임은 자체 CA 번들을 사용하므로 mkcert 인증서를 신뢰하지 않을 수 있음.
# 개발 환경에 한해 TLS 인증서 검증을 비활성화 (프로덕션에서는 절대 사용 금지)
NODE_TLS_REJECT_UNAUTHORIZED=0
